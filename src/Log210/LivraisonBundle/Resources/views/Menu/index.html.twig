{% extends 'Log210LivraisonBundle:Menu:layout.html.twig' %}

{% block body -%}
    <h1>Menu list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('menu_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <td>
                    <a class="glyphicon glyphicon-cd" href="{{ path('menu_show', { 'id': entity.id }) }}" rel="tooltip" title="Afficher"></a>
                    <a class="glyphicon glyphicon-edit" href="{{ path('menu_edit', { 'id': entity.id }) }}" rel="tooltip" title="'edit'|trans|capitalize"></a>
                    <a class="glyphicon glyphicon-remove"
                       onclick="return confirm('Voulez-vous vraiment supprimer ce restaurant?')"
                       rel="tooltip"
                       title="'delete'|trans|capitalize">
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('menu_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
